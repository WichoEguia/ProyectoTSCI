<%= form_with model: @article, local: true do |f| %>
<% if @article.errors.any? %>
<div id="error_explaination">
  <h2>
    <%= pluralize @article.errors.count, "error" %> prohibited this article from beign saved:
  </h2>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div>
  <%= f.label :title %><br>
  <%= f.text_field :title %>
</div>

<div>
  <%= f.label :text %><br>
  <%= f.text_area :text %>
</div>

<div>
  <%= f.submit %>
</div>
<% end %>